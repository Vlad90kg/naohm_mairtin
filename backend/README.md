# Backend

This folder is reserved for the future Django backend.

Suggested next steps in PyCharm:

1. Create a Python virtual environment inside or alongside `backend/`.
2. Install Django.
3. Start a Django project in this folder.
4. Add backend-specific dependency and environment files as needed.

The frontend already lives in `frontend/` and can be run independently from the repo root with `npm run dev`.
