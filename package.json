{
  "name": "homepage-wireframe-for-gaa-club",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "build:frontend": "npm run build --workspace frontend"
  }
}
